#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../swiffy-convert/bin/swiffy-convert" "$@"
  ret=$?
else 
  node  "$basedir/../swiffy-convert/bin/swiffy-convert" "$@"
  ret=$?
fi
exit $ret
